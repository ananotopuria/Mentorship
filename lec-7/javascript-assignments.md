# JavaScript Assignments

## Variable Naming Conventions

## 1. Variables — Descriptive Names

შექმენი ცვლადები შემდეგი ინფორმაციისთვის და **თავად გადაწყვიტე, რა იქნება თითოეული ცვლადის კარგი და აღწერითი სახელი**:

- მომხმარებლის სახელი → `"Nika"`
- მომხმარებლის ასაკი → `22`
- პროდუქტის ფასი → `49.99`
- პროდუქტი გაყიდულია თუ არა → `false`
- კალათაში პროდუქტების რაოდენობა → `4`

---

## 2. პატარა რეალური ამოცანა — Online Shop

ონლაინ მაღაზიაში ერთი მაისური ღირს **40 ლარი** და მომხმარებელი ყიდულობს **3 ცალს**.

შექმენი სწორად და აღწერითად დასახელებული ცვლადები და გამოთვალე **სრული ფასი**.

### დამატებითი დავალება

ახლა გვაქვს **20 ლარის ფასდაკლება**.

დაამატე ახალი ცვლადი ფასდაკლებისთვის და გამოთვალე **საბოლოო ფასი ფასდაკლების შემდეგ**.

> **მნიშვნელოვანია:** ცვლადების სახელები თავად მოიფიქრე. გამოიყენე აღწერითი სახელები და `camelCase`.

---

## 3. Country Information

1. შექმენი ცვლადები სახელებით:

   - `country`
   - `continent`
   - `population`

2. თითოეულ ცვლადს მიანიჭე შენი ქვეყნის შესაბამისი მნიშვნელობა.

> `population` მიუთითე **მილიონებში**.

3. `console.log()`-ის გამოყენებით გამოიტანე სამივე ცვლადის მნიშვნელობა კონსოლში.

```js
// Declare your variables here


// Log the values to the console
```

---

## Data Types

## 4. Identify the Data Types

მიუწერე თითოეულ მნიშვნელობას შესაბამისი მონაცემთა ტიპი:

```js
"Hello JavaScript"
25
49.99
true
false
undefined
null
123n
Symbol("id")
```

---

## 5. Create Variables with Different Data Types

შექმენი ცვლადები შემდეგი ინფორმაციის შესანახად. თავად შეარჩიე აღწერითი სახელები:

- სტუდენტის სახელი → `"Nino"`
- სტუდენტის ასაკი → `20`
- სტუდენტი აქტიურია თუ არა → `true`
- სტუდენტის მისამართი ჯერ უცნობია → `undefined`
- სტუდენტის მეორე ტელეფონის ნომერი არ არსებობს → `null`
- ძალიან დიდი რიცხვი → `9007199254740993n`
- უნიკალური სტუდენტის ID → `Symbol("studentId")`

გამოიყენე `const` ან `let` საჭიროების მიხედვით.

---

## 6. Check Data Types with `typeof`

შექმენი ქვემოთ მოცემული ცვლადები და `typeof` ოპერატორის გამოყენებით გამოიტანე თითოეულის მონაცემთა ტიპი კონსოლში:

```js
const productName = "Laptop";
const productPrice = 2500;
const isAvailable = true;
let deliveryDate;
const productCode = 123n;
const productId = Symbol("productId");
```

მაგალითის ფორმატი:

```js
console.log(typeof productName);
```

ასევე შეამოწმე:

```js
console.log(typeof null);
```

დააკვირდი, რა შედეგს დააბრუნებს JavaScript.

---

## Let and Const

## 7. `let` or `const`?

შექმენი ცვლადები შემდეგი ინფორმაციისთვის და თავად გადაწყვიტე, სად გამოიყენებ `let`-ს და სად `const`-ს:

- მომხმარებლის სახელი → `"Nika"`
- მომხმარებლის ასაკი → `22`
- დაბადების წელი → `2004`
- მიმდინარე ქულა → `10`
- ქვეყნის სახელი → `"Georgia"`

შემდეგ შეცვალე:

- მომხმარებლის ასაკი → `23`
- მიმდინარე ქულა → `15`

---

## 8. Shopping Cart

პროდუქტის ფასი არის **50 ლარი**, ხოლო კალათაში თავდაპირველად არის **2 პროდუქტი**.

1. შექმენი ფასისა და რაოდენობის ცვლადები.
2. გამოთვალე სრული ფასი.
3. შეცვალე პროდუქტების რაოდენობა `3`-ით.
4. ხელახლა გამოთვალე სრული ფასი.
5. გამოიტანე შედეგი კონსოლში.

> თავად გადაწყვიტე, რომელი ცვლადი უნდა გამოცხადდეს `let`-ით და რომელი — `const`-ით.

---

## 9. Fix the Errors

იპოვე და გაასწორე შეცდომები:

```js
const userAge = 20;
userAge = 21;

let country = "Georgia";
let country = "France";

const productPrice;
productPrice = 50;
```

---

## 10. Game Score

მოთამაშის სახელი თამაშის განმავლობაში არ იცვლება, მაგრამ მისი ქულა იცვლება.

1. შექმენი მოთამაშის სახელის ცვლადი მნიშვნელობით `"Nika"`.
2. შექმენი ქულის ცვლადი მნიშვნელობით `0`.
3. ქულას დაუმატე `10`.
4. შემდეგ ქულას დაუმატე კიდევ `5`.
5. გამოიტანე მოთამაშის სახელი და საბოლოო ქულა კონსოლში.

> გამოიყენე `const` უცვლელი მნიშვნელობისთვის და `let` ცვლადი მნიშვნელობისთვის.

---

## Operators

## 11. Arithmetic Operators

შექმენი ორი ცვლადი მნიშვნელობებით `20` და `4`. გამოთვალე და გამოიტანე კონსოლში:

- რიცხვების ჯამი;
- სხვაობა;
- ნამრავლი;
- განაყოფი;
- გაყოფის ნაშთი.

---

## 12. Assignment Operators

შექმენი `score` ცვლადი საწყისი მნიშვნელობით `10`.

1. `+=` ოპერატორით დაუმატე `5`.
2. `-=` ოპერატორით გამოაკელი `3`.
3. `*=` ოპერატორით გაამრავლე `2`-ზე.
4. `/=` ოპერატორით გაყავი `4`-ზე.
5. ყოველი მოქმედების შემდეგ გამოიტანე `score` კონსოლში.

---

## 13. Comparison and Logical Operators

შექმენი შემდეგი ცვლადები:

```js
const userAge = 20;
const hasTicket = true;
```

შეამოწმე და გამოიტანე კონსოლში:

- არის თუ არა მომხმარებელი `18` წლის ან მეტის;
- უდრის თუ არა ასაკი ზუსტად `20`-ს;
- არ უდრის თუ არა ასაკი `25`-ს;
- არის თუ არა მომხმარებელი სრულწლოვანი **და** აქვს ბილეთი;
- არის თუ არა მომხმარებელი არასრულწლოვანი **ან** არ აქვს ბილეთი.

---

## Strings

## 14. Create and Combine Strings

შექმენი ორი ცვლადი:

```js
const firstName = "Nika";
const lastName = "Beridze";
```

`+` ოპერატორის გამოყენებით გააერთიანე სახელი და გვარი. მათ შორის დაამატე გამოტოვება და შედეგი გამოიტანე კონსოლში.

---

## 15. String Length

შექმენი ცვლადი მნიშვნელობით:

```js
const programmingLanguage = "JavaScript";
```

1. გამოიტანე სტრინგი კონსოლში.
2. `.length`-ის გამოყენებით გამოიტანე სიმბოლოების რაოდენობა.
3. გამოიტანე სტრინგის პირველი და ბოლო სიმბოლო.

---

## 16. Change the String Case

შექმენი ცვლადი:

```js
const message = "Hello JavaScript";
```

1. `toUpperCase()`-ის გამოყენებით გადააქციე ტექსტი დიდ ასოებად.
2. `toLowerCase()`-ის გამოყენებით გადააქციე ტექსტი პატარა ასოებად.
3. ორივე შედეგი გამოიტანე კონსოლში.

---

## Template Literals

## 17. Personal Introduction

შექმენი ცვლადები მომხმარებლის სახელის, ასაკისა და ქალაქისთვის:

```js
const userName = "Nino";
const userAge = 21;
const city = "Tbilisi";
```

Template Literal-ის გამოყენებით შექმენი და გამოიტანე შემდეგი წინადადება:

```text
My name is Nino, I am 21 years old and I live in Tbilisi.
```

---

## 18. Product Information

შექმენი პროდუქტის სახელის, ფასისა და რაოდენობის ცვლადები. გამოთვალე სრული ღირებულება.

Template Literal-ის გამოყენებით გამოიტანე შემდეგი ფორმატის ტექსტი:

```text
You bought 3 T-shirts. The total price is 120 GEL.
```

---

## 19. Multiline Message

Template Literal-ის გამოყენებით შექმენი მრავალსტრიქონიანი ტექსტი შემდეგი ფორმატით:

```text
Order Summary
Product: Laptop
Price: 2500 GEL
Status: Available
```

მნიშვნელობები წინასწარ შეინახე ცალკე ცვლადებში და ტექსტში ჩასვი `${}`-ის გამოყენებით.

---

## Control Structures — `if/else`

## 20. Adult or Minor

შექმენი `age` ცვლადი.

- თუ ასაკი არის `18` ან მეტი, გამოიტანე `"You are an adult"`.
- სხვა შემთხვევაში გამოიტანე `"You are a minor"`.

ამოხსნისთვის გამოიყენე მხოლოდ `if/else`.

---

## 21. Positive or Negative Number

შექმენი `number` ცვლადი.

- თუ რიცხვი მეტია ან ტოლია `0`-ის, გამოიტანე `"The number is positive"`.
- სხვა შემთხვევაში გამოიტანე `"The number is negative"`.

ამოხსნისთვის გამოიყენე მხოლოდ `if/else`.

---

## 22. Login Check

შექმენი შემდეგი ცვლადები:

```js
const correctPassword = "js123";
const enteredPassword = "js123";
```

- თუ შეყვანილი პაროლი სწორ პაროლს ემთხვევა, გამოიტანე `"Login successful"`.
- სხვა შემთხვევაში გამოიტანე `"Incorrect password"`.

ამოხსნისთვის გამოიყენე მხოლოდ `if/else` და მკაცრი შედარების ოპერატორი.

---

## Truthy and Falsy Values

## 23. Convert Values to Boolean

`Boolean()` ფუნქციის გამოყენებით გადააქციე შემდეგი მნიშვნელობები Boolean ტიპად და შედეგები გამოიტანე კონსოლში:

```js
0
1
""
"Hello"
null
undefined
NaN
```

თითოეულ შედეგთან მიუწერე კომენტარი — `truthy` თუ `falsy`.

---

## 24. Username Check

შექმენი `userName` ცვლადი.

- თუ `userName` შეიცავს მნიშვნელობას, გამოიტანე `"Welcome, user!"`.
- თუ `userName` ცარიელი სტრინგია, გამოიტანე `"Username is required"`.

პირობაში პირდაპირ გამოიყენე `userName` ცვლადი:

```js
if (userName) {
  // Write your code here
} else {
  // Write your code here
}
```

---

## 25. Shopping Cart Check

შექმენი `cartItemsCount` ცვლადი.

- თუ კალათაში პროდუქტების რაოდენობა truthy მნიშვნელობაა, გამოიტანე `"Your cart has products"`.
- სხვა შემთხვევაში გამოიტანე `"Your cart is empty"`.

შეამოწმე დავალება ჯერ `cartItemsCount = 3` მნიშვნელობით, შემდეგ კი `cartItemsCount = 0` მნიშვნელობით.
